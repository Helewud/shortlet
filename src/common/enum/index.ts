export enum NodeEnv {
  DEV = 'dev',
  PROD = 'prod',
}

export enum ProfileRoles {
  CLIENT = 'client',
  CONTRACTOR = 'contractor',
}

export enum ContractStatus {
  NEW = 'new',
  IN_PROGRESS = 'in_progress',
  TERMINATED = 'terminated',
}

export enum ProviderTokens {
  TYPEORM_DATA_SOURCE = 'TYPEORM_DATA_SOURCE',
}
